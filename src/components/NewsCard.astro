---
interface Props {
    news: any;
}

const { news } = Astro.props as Props;
---

<a href={`/news/${ news.slug}`}>
    <div class="content">
        <p class="date">
            {news.data.date.toLocaleDateString('ja-JP', {
                year: 'numeric',
                month: '2-digit',
                day: '2-digit',
            }).replace(/\//g, '.')}
        </p>
        <p class="type">{news.data.tags.join(' ')}</p>
        <h3 class="title">{news.data.title}</h3>
    </div>
</a>

<style>
    .content {
        font-family: 'Noto Sans JP Variable', sans-serif;
		padding: 5px;
		border-bottom: 1px solid #B8B8B8;
	}

	.date, .type{
		font-weight: 500;
		font-size: 10px;
		letter-spacing: 0.4px;
		color: #000;
		margin: 0 16px 0 0;
		display: inline-block;
		position: relative;
	}

	.type::before {
		content: "";
		background-color: #A6A6A6;
		width: 6px;
		height: 6px;
		border-radius: 50%;
		position: absolute;
		left: -14px;
		top: 50%;
		transform: translateY(-50%);
	}

	.title {
		font-weight: 500;
		font-size: 15px;
		margin: 12px 0;
	}
</style>